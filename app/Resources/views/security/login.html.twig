<!DOCTYPE html>
<html>
<head>
    <title>Login Page</title>
   <!--Made with love by Mutiullah Samim -->
   
    <!--Bootsrap 4 CDN-->
    <link rel="stylesheet" href="{{ asset ('assets/bower_components/bootstrap/dist/css/bootstrap.min.css')}}">
    <!--Custom styles-->
    <link rel="stylesheet" href="{{ asset ('assets/bower_components/bootstrap/dist/css/custom.css')}}">
    <link rel="stylesheet" href="{{ asset ('assets/css/fontawesome.css')}}">


</head>

<body class="login-page">
   
            <div class="container">
                <div class="body-card text-center">
                    <div class="member-login">Sign In</div>
                        {% if error %}
                            <div>{{ error.messageKey|trans(error.messageData, 'security') }}</div>
                        {% endif %}
                        <form action="{{ path('login') }}" method="post">
                        <img class="man-user" src="{{ asset('assets/dist/img/man-user.png')}}" alt="product profile picture">
                        <input type="text" id="username" placeholder="Username" name="_username" value="{{ last_username }}" />
                        <br>
                        <img class="lock" src="{{ asset('assets/dist/img/lock.png')}}" alt="product profile picture">
                        <input type="password" id="password" name="_password" placeholder="Password" />
                        <br>
                        <button type="submit">login</button>
                        </form>
                </div>

                <!-- login -->

  


        
</body>
</html>

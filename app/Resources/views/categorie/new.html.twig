{% extends 'base.html.twig' %}

{% block body %}

        <h1>Categorie creation</h1>

<div class="box box-primary">
        <div class="box-header with-border">
          <h3 class="box-title">Add new categorie</h3>
        </div>
    <!-- /.box-header -->
    <!-- form start -->
            {{ form_start(form) }}
        <div class="box-body">
            <div class="form-group">
            {{ form_widget(form) }}
                <div class="box-footer">
                    <input type="submit" class="btn btn-block btn-info btn-flat" style="width: 125px; margin-bottom: 5px;" value="Create" />
                    <a class="btn btn-default btn-block btn-flat" style="width: 125px;"" href="{{ path('product_index') }}"><i class="fa fa-repeat"></i>Back to the list</a>
                </div>
            </div>     
        </div>
            {{ form_end(form) }}
</div-->

{% endblock %}
